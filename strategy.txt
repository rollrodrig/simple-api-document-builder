. create html template with cherio
. read api folder
. store in array all the json files
. loop array and remove _config.js key and store in a variable
. read _config.json values
    . create document html title and version
    . store html in memory
. loop the json array
. [arrage the json array by number prefix]
. loop json array
    .create top sidebar html
    .create bottom sidebar html
    . each api file
        . create top list html
        . append name and link
        . create bottom list and append
    .store on memmory

. loop json array
    .read one json
    . create top html and store on memory
        . append sidebar from memory
        . append top 2
        . store on memory
    . create bottom html and store on memory
    . create content html
        . convert to json the json
        . loop each key
            . create html for
                .title
                .description
                .method
                .path
                
            .read parameters array
                . create table wrapper
                . create table titles
                . loop each parameter
                    . create each row
                . create table bottom
                . append table bottom

            .read request
                . create block top html
                . append json block
                . create and bottom html tag

            .read response
                . create block top html
                . append json block
                . create and bottom html tag

            .read error
                . create block top html
                . append json block
                . create and bottom html tag                

    . append "bottom"